# spam-classifier
A Java-based classification tree (decision tree) that predicts whether a message is Spam or Ham based on text features. Demonstrates foundations of machine learning, including training, prediction, evaluation, and model serialization — all implemented from scratch without ML libraries.
